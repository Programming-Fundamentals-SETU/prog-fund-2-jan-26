import java.util.ArrayList;
public class BoxAPI {
    ArrayList<Box> boxes = new ArrayList<Box>();
    //todo
    //write adding a Box object to boxes (addBox(Box box))
    // write remove a Box object from boxes (index of box to be removed as parameter) - check that the index is valid
    //write a listBoxes that returns a String containing details of all boxes

}
