#SalesWorker class

The UML for the manager class is here:

![](img/SalesWorker.png)

The responsibility for this (concrete) class is to extend Employee and implement the class for a Sales Worker

#Fields

There is one private fields in the Manager class and is described as follows:

- *percentBonus*: This is a percent value (between 0 and 20% inclusive) 

##Constructor

There is one constructor that should have the same method signature as the diagram above.  The constructor should call the superclass consructor and also instantiate the percentBonus (with validation)


##Methods (getters and setters)

The above field should have a getter and setter.  Use the validation rules for the setter. 


##calculateSalary()
This takes the salary calculated by the superclass's **getSalary**  and adds it to the Sales Worker's bonus, defined as follows: 
The Sales Worker's bonus is calculated by the percentage bonus amount to  his/her full salary(including their bonuses). For example if the full salary = 100 and the percentBonus = 13%, then the final salary = 113)

